{{- with .Values -}}
{{ if .enable.env_configmap }}
kind: ConfigMap
apiVersion: v1
metadata:
  name: hidebound-env-configmap
  namespace: {{ .namespace.name }}
data:
{{- range $key, $val := .env_configmap -}}
  {{ $key | nindent 2 }}: "{{ $val }}"
{{- end }}
{{- end -}}
{{- end -}}
